# Local overrides — not committed by default (see .cursorignore/.gitignore).
# Leave proxy blank so `npm run dev` uses per-service proxies in vite.config.ts → localhost:3001 (auth).
# Uncomment ONLY if you run the unified gateway:
# VITE_PROXY_GATEWAY=http://127.0.0.1:3000
