import { RegisterWizard } from "./RegisterWizard";

/** LinkedIn-style multi-step onboarding; business logic lives in `RegisterWizard`. */
export function RegisterPage() {
  return <RegisterWizard />;
}
